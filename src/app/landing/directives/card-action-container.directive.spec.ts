import { CardActionContainerDirective } from './card-action-container.directive';

describe('CardActionContainerDirective', () => {
  it('should create an instance', () => {
    const directive = new CardActionContainerDirective();
    expect(directive).toBeTruthy();
  });
});
